{
  "dependencies": [
    {
      "name": "node",
      "description": "Node.js runtime — required for QMD and npx",
      "check": "node --version",
      "required": true,
      "install": {
        "darwin": "brew install node",
        "linux-apt": "sudo apt install nodejs npm",
        "linux-dnf": "sudo dnf install nodejs npm",
        "windows": "winget install OpenJS.NodeJS",
        "manual": "https://nodejs.org/en/download"
      }
    },
    {
      "name": "python3",
      "description": "Python 3 — required for scheduler and background jobs",
      "check": "python3 --version",
      "required": true,
      "install": {
        "darwin": "brew install python3",
        "linux-apt": "sudo apt install python3 python3-venv",
        "linux-dnf": "sudo dnf install python3",
        "windows": "winget install Python.Python.3.12",
        "manual": "https://www.python.org/downloads/"
      }
    },
    {
      "name": "qmd",
      "description": "Semantic search over markdown files (embedding + vector search)",
      "check": "npx @tobilu/qmd --version",
      "required": true,
      "install": {
        "all": "npm install -g @tobilu/qmd"
      },
      "setup": {
        "collection": "npx @tobilu/qmd collection add ~/Documents/Knowledge --name knowledge",
        "context": "npx @tobilu/qmd context add qmd://knowledge \"Personal knowledge base: inbox items (ideas, tasks, notes, reports, processes), projects, people, companies, meeting notes\"",
        "embed": "npx @tobilu/qmd embed"
      },
      "mcp": {
        "name": "qmd",
        "command": "claude mcp add --scope user qmd -- npx @tobilu/qmd mcp"
      }
    },
    {
      "name": "jq",
      "description": "JSON processor — required for status line",
      "check": "jq --version",
      "required": true,
      "install": {
        "darwin": "brew install jq",
        "linux-apt": "sudo apt install jq",
        "linux-dnf": "sudo dnf install jq",
        "windows": "winget install jqlang.jq",
        "manual": "https://jqlang.github.io/jq/download/"
      }
    },
    {
      "name": "git",
      "description": "Version control — Kay repo is git-tracked",
      "check": "git --version",
      "required": true,
      "install": {
        "darwin": "xcode-select --install",
        "linux-apt": "sudo apt install git",
        "linux-dnf": "sudo dnf install git",
        "windows": "winget install Git.Git",
        "manual": "https://git-scm.com/downloads"
      }
    },
    {
      "name": "gh",
      "description": "GitHub CLI — used for repo operations and PR workflows",
      "check": "gh --version",
      "required": false,
      "install": {
        "darwin": "brew install gh",
        "linux-apt": "sudo apt install gh",
        "linux-dnf": "sudo dnf install gh",
        "windows": "winget install GitHub.cli",
        "manual": "https://cli.github.com/"
      }
    }
  ]
}
